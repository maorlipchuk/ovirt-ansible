- name: Run highly available VMs
  ovirt_vms:
      state: running
      name: "{{ vms.name }}"
      wait: False
      auth: "{{ ovirt_auth }}"
      # TODO: Add support for pin to host: Should the host be predefine?
      #host: "ovirt_hosts[0].name"
